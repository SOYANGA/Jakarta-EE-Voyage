Mybatis
	MyBatis与JDBC
		JDBC的执行流程
		阻抗不匹配
	Mybatis的配置
		XML配置
		属性(properties)
		settings核心配置
		配置日志
		类型别名(typeAliases)
			默认内建类型别名
			自定义类型别名
		类型处理器(typeHandlers)
			默认的类型处理器
			 自定义类型处理器
		对象工厂(ObjectFactory)
		插件(plugins)
		配置环境(enviornments)
		事务管理
		数据源(dataSource)
		映射器(mappers)
	Mybatis的Mapper映射
		Mapper文件结构
		sekect命令
		Insert Update Delete
		sql
		parameter 
			基本类型
			引用类型
			字符串
		resultMap(查询结果映射) 
		cache(缓存)
			一级缓存
			二级缓存
			缓存属性
			自定义缓存
			第三方缓存
	Mybatis动态SQL映射
		动态SQL简介
		动态SQL标记
			if
			where
			choose,when,otherwise
			trim,where,set
			foreach
		分页插件
